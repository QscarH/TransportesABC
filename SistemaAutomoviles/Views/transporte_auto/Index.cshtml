@model IEnumerable<SistemaAutomoviles.Models.transporte_auto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Automovil.FolioAuto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Automovil.Marca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Solicitud_de_transporte.NumLote)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <th>
            @Html.DisplayFor(modelItem => item.Automovil.FolioAuto)
        </th>
        <td>
            @Html.DisplayFor(modelItem => item.Automovil.Marca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Solicitud_de_transporte.NumLote)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
            @Html.ActionLink("Details", "Details", new { id = item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id })
        </td>
    </tr>
}

</table>
